function  onChangeUsuario(evento){
    // obtener el input "txtUsuario"
    let miUsuario document.getElementById("txtUsuario")
    // guardar el contenido del input (value) en una variable
    let usuarioEscrito = miUsuario.value
    //mostrar la variable del contenido
    console.log("Usuario:", );
}